# use gradle standard settings and cache
mkdir -p "$HOME"/.gradle
docker run -it --rm -v "$HOME"/.gradle:/root/.gradle -v "$PWD":/work -w /work eclipse-temurin:17 ./gradlew --no-daemon clean publishToMavenLocal
ls -l build/libs